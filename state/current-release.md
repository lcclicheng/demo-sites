# State · Current Release

- **最近提交**：`8b0233d`（2026-07-16）
- **内容**：MDD 第二轮——超级单文件 `workflow.md` 拆为 architecture/deployment/onboarding/seo 主题文件；docs/index 改纯索引；workflow.md 归档。
- **上一提交**：`f2281b4`（MDD 骨架：memory/decisions/tasks/.ai）+ `3292658`（PROJECT-OVERVIEW 整合版）。
- **分支**：`main`（已推送 `origin/main`）。
- **发布节奏**：GitHub Pages 子路径 `/demo-sites/`，SSH 推送即触发 Actions 自动校验→构建→部署（无手动 release tag 流程；如需 tag 见 `checklists/release.md`）。
- **更新时机**：每次 `git push` 成功后由 AI 同步 commit hash + 日期。
